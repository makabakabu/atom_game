import React from 'react';

const PreviewBoard = () => (
    <div style={styles.main} />
);

const styles = {
    main: {
        width: '100%',
        height: '78%',
        backgroundColor: '#ededed',
    },
};

export default PreviewBoard;
